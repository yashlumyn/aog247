Run the PartSense API server by activating the virtual environment and starting uvicorn.

Execute the following command using the Bash tool:

```bash
source .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000
```
